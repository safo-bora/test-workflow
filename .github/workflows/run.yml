name: Run selected environments

run-name: "${{ inputs.test_type }} on ${{ inputs.registry_env_names || inputs.environment_selection }} by @${{ github.actor }}"

on:
  workflow_dispatch:
    inputs:
      environment_selection:
        description: "Select one environment"
        type: choice
        required: true
        options:
          - test-blue-01
          - test-blue-02
          - test-blue-03
          - test-blue-04
          - test-blue-05
          - test-blue-06
          - test-blue-07
          - test-blue-08
          - test-blue-09
          - test-blue-10
          - test-blue-11
          - test-blue-12
          - test-blue-13
          - test-blue-14
          - test-blue-15
          - test-blue-16
          - test-blue-17
          - test-blue-18
          - test-blue-19
          - test-blue-20
          - test-blue-21
          - test-blue-22
          - test-blue-23
          - test-blue-24
          - test-blue-25
          - test-green-01
          - test-green-02
          - test-green-03
          - test-green-04
          - test-green-05
          - test-green-06
          - test-green-07
          - test-green-08
          - test-green-09
          - test-green-10
          - test-green-11
          - test-green-12
          - test-green-13
          - test-green-14
          - test-green-15
          - test-green-16
          - test-green-17
          - test-green-18
          - test-green-19
          - test-green-20
          - test-green-21
          - test-green-22
          - test-green-23
          - test-green-24
          - test-green-25
          - test-purple-01
          - test-purple-02
          - test-purple-03
          - test-purple-04
          - test-purple-05
          - test-purple-06
          - test-purple-07
          - test-purple-08
          - test-purple-09
          - test-purple-10
          - test-purple-11
          - test-purple-12
          - test-purple-13
          - test-purple-14
          - test-purple-15
          - test-purple-16
          - test-purple-17
          - test-purple-18
          - test-purple-19
          - test-purple-20
          - test-purple-21
          - test-purple-22
          - test-purple-23
          - test-purple-24
          - test-purple-25
          - test-orange-01
          - test-orange-02
          - test-orange-03
          - test-orange-04
          - test-orange-05
          - test-orange-06
          - test-orange-07
          - test-orange-08
          - test-orange-09
          - test-orange-10
          - test-orange-11
          - test-orange-12
          - test-orange-13
          - test-orange-14
          - test-orange-15
          - test-orange-16
          - test-orange-17
          - test-orange-18
          - test-orange-19
          - test-orange-20
          - test-orange-21
          - test-orange-22
          - test-orange-23
          - test-orange-24
          - test-orange-25

      registry_env_names:
        description: "Multiple environments, comma-separated. Leave empty to use the dropdown."
        type: string
        required: false

      ui_feature_tag:
        description: "──────── TEST SETTINGS ──────── UI feature folder"
        type: choice
        required: true
        options:
          - all
          - obligations
          - matters
          - search

      test_type:
        description: "Test type to run. Use infer-from-selector when a test case ID is provided."
        type: choice
        required: true
        options:
          - infer-from-selector
          - api-schema
          - api-regression
          - ui-regression
          - fmp-ui-regression

      feature_tag:
        description: "Test selector by tag. Select all to run all tests."
        type: choice
        required: true
        options:
          - all
          - pr
          - smoke
          - nightly

      test_selector_id:
        description: "Test case ID(s), comma-separated"
        type: string
        required: false

      test_selector_title:
        description: "Test selector by title"
        type: string
        required: false

      thread_count:
        description: "Thread count for pytest-xdist"
        type: choice
        required: true
        options:
          - auto
          - "2"
          - "4"
          - "8"

      xray_test_plan_id:
        description: "Xray Test Plan ID (optional)"
        type: string
        required: false

      xray_test_execution_id:
        description: "Xray Test Execution ID (optional)"
        type: string
        required: false

permissions:
  contents: read

jobs:
  prepare:
    name: Prepare environment matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Install YAML parser
        run: python -m pip install --disable-pip-version-check PyYAML

      - name: Build environment matrix
        id: matrix
        env:
          WORKFLOW_INPUTS: ${{ toJSON(inputs) }}
        run: |
          python <<'PY'
          import json
          import os
          import sys

          import yaml

          with open("config/config-environments.yml", encoding="utf-8") as file:
              config = yaml.safe_load(file)

          available = {
              item["environment"]: item
              for environments in config.values()
              for item in environments
          }
          inputs = json.loads(os.environ["WORKFLOW_INPUTS"])
          manual_names = inputs["registry_env_names"].strip()
          if manual_names:
              selected = [
                  name.strip()
                  for name in manual_names.split(",")
                  if name.strip()
              ]
              selected = list(dict.fromkeys(selected))
          else:
              selected = [inputs["environment_selection"]]

          unknown = [name for name in selected if name not in available]
          if unknown:
              print(f"Missing from config: {', '.join(unknown)}", file=sys.stderr)
              sys.exit(1)

          matrix = [available[name] for name in selected]
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              output.write("matrix=" + json.dumps(matrix, separators=(",", ":")) + "\n")

          print(f"Selected {len(matrix)} environment(s): {', '.join(selected)}")
          PY

  debug:
    name: "${{ inputs.test_type }} — ${{ matrix.environment }}"
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        include: ${{ fromJSON(needs.prepare.outputs.matrix) }}

    steps:
      - name: Show mock test configuration
        env:
          ENVIRONMENT_NAME: ${{ matrix.environment }}
          BASE_URL: ${{ matrix.base_url }}
          BASE_API_URL: ${{ matrix.base_api_url }}
          TEST_TYPE: ${{ inputs.test_type }}
          FEATURE_TAG: ${{ inputs.feature_tag }}
          TEST_SELECTOR_ID: ${{ inputs.test_selector_id }}
          TEST_SELECTOR_TITLE: ${{ inputs.test_selector_title }}
          THREAD_COUNT: ${{ inputs.thread_count }}
          XRAY_TEST_PLAN_ID: ${{ inputs.xray_test_plan_id }}
          XRAY_TEST_EXECUTION_ID: ${{ inputs.xray_test_execution_id }}
          UI_FEATURE_TAG: ${{ inputs.ui_feature_tag }}
        run: |
          echo "Environment: ${ENVIRONMENT_NAME}"
          echo "Base URL: ${BASE_URL}"
          echo "API URL: ${BASE_API_URL}"
          echo "Test type: ${TEST_TYPE}"
          echo "Feature tag: ${FEATURE_TAG}"
          echo "Test case IDs: ${TEST_SELECTOR_ID}"
          echo "Test title: ${TEST_SELECTOR_TITLE}"
          echo "Threads: ${THREAD_COUNT}"
          echo "Xray Test Plan: ${XRAY_TEST_PLAN_ID}"
          echo "Xray Test Execution: ${XRAY_TEST_EXECUTION_ID}"
          echo "UI feature: ${UI_FEATURE_TAG}"
          echo "Mock run completed."
